# Phase 2: CLI層とコマンドシステムの理解

**前提フェーズ**: Phase 1
**状態**: :white_large_square: 未着手

## 目標

CLIのコマンド解析・ルーティング・依存性注入の仕組みを理解する。

## チェック項目

### 2.1 CLI基盤
- [ ] `src/cli/` ディレクトリの全体構成を確認する
- [ ] 引数パース（コマンドライン引数の解析）の仕組みを理解する
- [ ] コマンドルーティング（コマンド名→ハンドラ関数のマッピング）を理解する

### 2.2 依存性注入
- [ ] `createDefaultDeps()` の実装を見つけ、何が注入されるかを確認する
- [ ] 依存オブジェクトがコマンドハンドラにどのように渡されるかを追跡する

### 2.3 主要コマンドの実装パターン
- [ ] `src/commands/` ディレクトリの全体構成を確認する
- [ ] `gateway` コマンドの実装を読み、コマンドの標準的な実装パターンを理解する
- [ ] `agent` コマンドの実装を読む
- [ ] `config` コマンドの実装を読む
- [ ] `onboard` コマンドの実装を読む
- [ ] `doctor` コマンドの実装を読む

### 2.4 コマンド実行フロー
- [ ] `openclaw gateway --port 18789` の処理フローをエントリポイントからコマンドハンドラまで追跡する
- [ ] `openclaw agent --message "hello"` の処理フローを追跡する

## 完了条件

> 「CLIコマンドがどのように登録・解析・実行されるか」を説明できる

## メモ

<!-- 理解した内容、疑問点、気づいたことをここに記録する -->

